services:
  backend:
    build:
      context: ./apps/backend
      dockerfile: DockerFile
    ports:
      - "3000:3000"
    env_file:
      - .env

  dashboard:
    build:
      context: ./apps/dashboard
      dockerfile: DockerFile
    ports:
      - "3001:3000"
    depends_on:
      - backend
